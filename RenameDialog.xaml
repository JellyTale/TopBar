<Window x:Class="TopBar.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Bar"
        Width="320" Height="140"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="NameBox"
                 Grid.Row="0"
                 FontSize="14"
                 Padding="6,4"
                 Background="{DynamicResource PopupInputBg}"
                 Foreground="{DynamicResource PopupInputFg}"
                 BorderBrush="{DynamicResource PopupInputBorder}"
                 CaretBrush="{DynamicResource PopupInputFg}" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="70" Height="28"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OK_Click"
                    Background="{DynamicResource PopupButtonBg}"
                    Foreground="{DynamicResource PopupInputFg}"
                    BorderBrush="{DynamicResource PopupInputBorder}" />
            <Button Content="Cancel"
                    Width="70" Height="28"
                    IsCancel="True"
                    Background="{DynamicResource PopupButtonBg}"
                    Foreground="{DynamicResource PopupInputFg}"
                    BorderBrush="{DynamicResource PopupInputBorder}" />
        </StackPanel>
    </Grid>
</Window>
